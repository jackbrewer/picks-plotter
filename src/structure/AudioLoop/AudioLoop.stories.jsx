import React from 'react'

import AudioLoop from './'

export default {
  title: 'Unsorted/AudioLoop',
  component: AudioLoop,
}

const Template = (args) => <AudioLoop />

export const Default = Template.bind({})
